import { EditarEstrategia as EditForm } from "@/components/component/editar_formulario";
function page() {
      return (
        <div>
          <EditForm />
        </div>
      );
}

export default page;