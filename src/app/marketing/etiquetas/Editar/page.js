import { DocumentSigningForm as Etiquetas } from "@/components/component/Editar_etiqueta";
function page() {
    
      return (
        <div>
          
          <Etiquetas />
        </div>
      );
}

export default page;